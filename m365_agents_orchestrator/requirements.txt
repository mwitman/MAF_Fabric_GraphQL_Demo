# M365 Agents SDK — pinned to 0.8.0 (matching GEV-SAA colleague's working versions)
python-dotenv
aiohttp==3.11.11
microsoft-agents-hosting-aiohttp==0.8.0
microsoft-agents-hosting-core==0.8.0
microsoft-agents-authentication-msal==0.8.0
microsoft-agents-activity==0.8.0

# Microsoft Agent Framework (MAF) — Responses API with hosted MCP tools
# Pinned to match local working versions; prevents server from resolving
# incompatible openai transitive deps that build wrong Azure OpenAI URLs.
agent-framework-azure-ai==1.0.0b251007
agent-framework-core==1.0.0rc3
openai==2.8.1

# Fabric authentication
azure-identity>=1.19.0
